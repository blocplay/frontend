import React from 'react';

const propTypes = {};

const defaultProps = {};

function Index() {
	return <div className="conversation__prompt">Click on a conversation.</div>;
}

Index.propTypes = propTypes;
Index.defaultProps = defaultProps;

export default Index;
